syntax = "proto3";

package rules;

import "rules/rule_config.proto";
import "transaction/transaction.proto";

message RuleRequest {
    transaction.Transaction transaction = 1;
    rules.RuleConfig rule = 2;
}