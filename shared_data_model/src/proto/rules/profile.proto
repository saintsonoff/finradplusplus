syntax = "proto3";

package profile;

import "google/protobuf/empty.proto";

import "rule_config.proto";

service ProfileService {
    rpc ProcessProfileStream(stream Profile) returns (google.protobuf.Empty);
}

message Profile {
    string uuid = 1;
    string name = 2;
    repeated rules.RuleConfig rules = 3;
}
